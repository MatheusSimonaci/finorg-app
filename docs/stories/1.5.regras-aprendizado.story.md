# STORY 1.5: Regras de Aprendizado

**ID:** 1.5 | **Epic:** [Epic 1 — Importação & Classificação IA](../prd/epic-1-importacao-classificacao.md)
**Points:** 3 | **Priority:** 🟠 High
**Created:** 2026-03-07
**Status:** 📋 Draft
**Predecessor:** Story 1.4 (revisão funcional)

---

## User Story

**Como** usuário,
**Quero** que minhas correções de classificação gerem regras permanentes,
**Para que** importações futuras sejam classificadas corretamente sem revisão manual repetitiva.

---

## Acceptance Criteria

- [ ] AC1: Ao corrigir uma classificação, o sistema apresenta: "Criar regra permanente para transações com '[descrição parcial]'?"
- [ ] AC2: Regras são salvas com: `pattern` (texto exato ou regex), `nature`, `category`, `type`, `subcategory`
- [ ] AC3: Regras têm prioridade total sobre a classificação IA (aplicadas antes de enviar para API, Story 1.3)
- [ ] AC4: Tela de configurações exibe lista de todas as regras salvas com opção de editar e deletar
- [ ] AC5: Regras pré-configuradas do sistema (CapCut, ferramentas, XP, Bitybank) são carregadas no seed inicial
- [ ] AC6: Sistema sugere `pattern` baseado na descrição da transação (ex: "IFOOD" → padrão sugerido: "IFOOD")
- [ ] AC7: Regras do tipo `regex` são suportadas (ex: `^PAGAMENTO PIX.*EMPRESA`)
- [ ] AC8: Indicador na tela de revisão mostrando se a classificação veio de uma regra ou da IA

---

## Dev Notes

### Regras pré-carregadas no seed

```typescript
const DEFAULT_RULES = [
  { pattern: 'CAPCUT|ADOBE|FIGMA|CANVA|NOTION|SLACK|LINEAR|CONTINUITY', nature: 'work_tool', category: 'assinatura', type: 'gasto' },
  { pattern: 'SALARIO|FOLHA PAGAMENTO', nature: 'pessoal', category: 'receita', type: 'receita' },
  { pattern: 'NUBANK RESERVAS|TESOURO DIRETO', nature: 'pessoal', category: 'investimento', type: 'investimento' },
]
```

### Aplicação de regras

```typescript
function applyRules(tx: NormalizedTransaction, rules: ClassificationRule[]): ClassificationResult | null {
  for (const rule of rules) {
    const regex = new RegExp(rule.pattern, 'i')
    if (regex.test(tx.description)) {
      return {
        nature: rule.nature,
        category: rule.category,
        type: rule.type,
        confidence: 1.0,
        reasoning: `Regra: "${rule.pattern}"`
      }
    }
  }
  return null
}
```

---

## Tasks

### T1 — Modal de criação de regra
- [ ] Modal disparado após correção de classificação com campo de pattern pré-preenchido
- [ ] Preview: "Esta regra se aplicaria a X transações no histórico"
- [ ] Botão "Criar regra" salva via `POST /api/classification-rules`

### T2 — Tela de gerenciamento de regras
- [ ] `app/settings/rules/page.tsx` com lista de regras
- [ ] Ações por regra: editar pattern/categoria, ativar/desativar, deletar
- [ ] Indicador de origem: "sistema" vs "usuário"

### T3 — Integração com classifier (Story 1.3)
- [ ] `lib/ai/rule-matcher.ts` já previsto na Story 1.3 — garantir que as regras do banco são carregadas na classificação

---

## Testing

| Test ID | Nome | Tipo | Prioridade |
|---------|------|------|-----------|
| T1.5-01 | Regra salva é aplicada na próxima importação | Integration | P0 |
| T1.5-02 | Regra regex funciona corretamente | Unit | P0 |
| T1.5-03 | Regras do seed carregadas na instalação | Integration | P0 |
| T1.5-04 | Deletar regra → não afeta classificações já feitas | Unit | P1 |

---

## File List

_Preenchido pelo @dev durante implementação_

---

## Change Log

| Data | Mudança | Autor |
|------|---------|-------|
| 2026-03-07 | Story criada | @pm |
