# STORY 4.3: Modo Sonho Ativo (Alocação Diferenciada)

**ID:** 4.3 | **Epic:** [Epic 4 — Sistema de Sonhos](../prd/epic-4-sistema-sonhos.md)
**Points:** 5 | **Priority:** 🟠 High
**Created:** 2026-03-07
**Status:** ✅ Done
**Predecessor:** Story 4.2 + Story 3.3

---

## User Story

**Como** usuário,
**Quero** que quando um sonho estiver ativo, o sistema ajuste a alocação-alvo recomendada da carteira,
**Para que** eu naturalmente concentre mais liquidez nos ativos certos para realizar o sonho.

---

## Acceptance Criteria

- [x] AC1: Quando há sonho ACUMULANDO, o módulo de investimentos exibe banner: "Sonho '[nome]' ativo — alocação ajustada para maximizar liquidez"
- [x] AC2: Visualização no banner com link para o painel do sonho
- [x] AC3: Módulo de investimentos destaca banner com valor reservado e aporte mensal necessário
- [x] AC4: Link direto para o painel do sonho ativo
- [x] AC5: Banner é dispensável sem alterar a alocação normal

---

## Dev Notes

### Lógica de alocação modo sonho

Quando um sonho está ativo, o `AllocationTarget` para o modo `sonho_ativo` é sugerido com base no `targetAmount` do sonho vs. o portfólio total:

```typescript
function suggestDreamAllocation(dreamAmount: number, portfolioTotal: number, normalTargets: AllocationTarget[]): AllocationTarget[] {
  const dreamPct = Math.min((dreamAmount / portfolioTotal) * 100, 40) // máx 40% em liquidez
  
  return normalTargets.map(t => {
    if (['conta_remunerada', 'tesouro'].includes(t.assetType)) {
      return { ...t, targetPct: t.targetPct + dreamPct / 2 }
    }
    // Reduz proporcionalmente os demais
    return { ...t, targetPct: t.targetPct * (1 - dreamPct / 200) }
  })
}
```

---

## Tasks

### T1 — Banner de modo sonho ativo
- [ ] `components/finance/dream-mode-banner.tsx`
- [ ] Exibido no topo do módulo de investimentos quando há sonho ACUMULANDO

### T2 — Visualização de alocação diferenciada
- [ ] Tabela comparativa: normal | modo sonho ativo | atual
- [ ] Highlight nos tipos afetados pela mudança

### T3 — Persistência da alocação modo sonho
- [ ] Ao confirmar sugestão, salva `AllocationTarget` com `mode=sonho_ativo` e `dreamId`

---

## Testing

| Test ID | Nome | Tipo | Prioridade |
|---------|------|------|-----------|
| T4.3-01 | Banner aparece apenas com sonho ACUMULANDO | Unit | P0 |
| T4.3-02 | Sugestão de alocação aumenta liquidez | Unit | P0 |
| T4.3-03 | Personalização do modo sonho não altera modo normal | Unit | P0 |

---

## File List

_Preenchido pelo @dev durante implementação_

---

## Change Log

| Data | Mudança | Autor |
|------|---------|-------|
| 2026-03-07 | Story criada | @pm |
