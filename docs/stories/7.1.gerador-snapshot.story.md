# STORY 7.1: Gerador de Snapshot

**ID:** 7.1 | **Epic:** [Epic 7 — Snapshot Mobile Estático](../prd/epic-7-snapshot-mobile.md)
**Points:** 5 | **Priority:** 🟠 High
**Created:** 2026-03-07
**Status:** 📋 Draft
**Predecessor:** Story 2.1 (dashboard central), Story 3.2 (visão de alocação)

---

## User Story

**Como** usuário,
**Quero** gerar um snapshot estático do meu dashboard financeiro com um clique,
**Para que** eu tenha uma versão offline e segura para consultar no celular sem expor o sistema principal.

---

## Acceptance Criteria

- [ ] AC1: Botão "📸 Gerar Snapshot" acessível no dashboard principal
- [ ] AC2: Geração concluída em menos de 5 segundos
- [ ] AC3: Output: pasta `/snapshot/` contendo HTML completamente auto-contido (CSS e dados embutidos via inline/JSON)
- [ ] AC4: Dados exportados: saldo atual, orçamento, portfólio, sonhos, reserva — snapshot da data atual
- [ ] AC5: Timestamp de geração exibido no snapshot: "Gerado em DD/MM/YYYY HH:mm"
- [ ] AC6: Feedback de progresso ao usuário durante a geração (spinner/progress bar)
- [ ] AC7: Nenhum dado de API key ou credencial incluído no snapshot

---

## Dev Notes

### Estratégia de geração
```typescript
// lib/snapshot/generator.ts
// 1. Buscar todos os dados necessários via queries Prisma
// 2. Serializar para JSON embarcado no HTML (sem fetch externo)
// 3. Escrever /snapshot/index.html com template auto-contido
// 4. CSS e charts via SVG inline (sem CDN)

async function generateSnapshot(outputDir: string): Promise<void> {
  const data = await collectSnapshotData(); // queries Prisma
  const html = renderSnapshotTemplate(data);
  await fs.writeFile(path.join(outputDir, 'index.html'), html, 'utf-8');
}
```

### API route
```
POST /api/snapshot/generate
Response: { path: "/snapshot/index.html", generatedAt: ISO_DATE }
```

---

## Tasks

### T1 — Coletor de dados
- [ ] `lib/snapshot/collector.ts` — agrega todos os dados para o snapshot
- [ ] Garantir exclusão de dados sensíveis (API keys, tokens)

### T2 — Template HTML auto-contido
- [ ] `lib/snapshot/template.ts` — template com dados embutidos como `window.__SNAPSHOT_DATA__`
- [ ] CSS inline (sem external stylesheet)

### T3 — API route
- [ ] `app/api/snapshot/generate/route.ts` — POST, escreve arquivo, retorna path

### T4 — UI trigger
- [ ] Botão "📸 Gerar Snapshot" no header do dashboard
- [ ] Feedback: spinner durante geração, toast de sucesso com link

---

## Testing

| Test ID | Nome | Tipo | Prioridade |
|---------|------|------|-----------|
| T7.1-01 | Snapshot gerado em < 5 segundos | Integration | P0 |
| T7.1-02 | HTML output é válido e auto-contido (sem URLs externas) | Integration | P0 |
| T7.1-03 | Nenhuma credencial no output | Security | P0 |

---

## File List

_Preenchido pelo @dev durante implementação_

---

## Change Log

| Data | Mudança | Autor |
|------|---------|-------|
| 2026-03-07 | Story criada | @pm |
